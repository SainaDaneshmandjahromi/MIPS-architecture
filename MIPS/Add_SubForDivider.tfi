


// Instantiate the module
Add_SubForDivider instance_name (
    .clk(clk), 
    .startbit(startbit), 
    .srOraddsub(srOraddsub), 
    .iteration(iteration), 
    .r(r), 
    .d(d), 
    .rsubd(rsubd)
    );


